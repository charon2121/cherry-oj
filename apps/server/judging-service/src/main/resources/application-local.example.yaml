# 复制为 application-local.yaml，填写本地值。空凭据必须填写，不能直接运行。
# 服务 token 至少 32 位 URL-safe 字符；发送/接收值必须配对，详见 README。
spring:
  kafka:
    bootstrap-servers: "127.0.0.1:9092"
  datasource:
    url: "jdbc:mysql://127.0.0.1:3306/cherry_oj_judging?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC"
    username: "cherry_oj_judging"
    password: ""
cherry:
  service-calls:
    submission-judging-tokens: ""
  formal:
    enabled: "true"
    submission-url: "http://127.0.0.1:8083"
    submission-token: ""
  judging:
    node:
      control-token: ""
