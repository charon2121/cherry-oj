---
id: "PLAN-034"
type: "plan"
title: "将沙箱已验收回归固化为重构 CI"
status: "checked"
work: "WORK-050"
owners: ["codex/root"]
depends_on: ["CAPABILITY-008", "EXPERIENCE-021", "DESIGN-044", "DECISION-028"]
related: []
implements: []
verifies: []
tags: []
created_at: "2026-09-10"
updated_at: "2026-09-10"
---

# PLAN-034：先固化回归，再承接重构

## 目标

将 WORK-048 实测范围转成可重复、不会假绿的 CI 基线，作为 WORK-049 实施与最终验收依据。

## 改动区域

.github/workflows/ci.yml、新增 deploy/sandbox-linux/ci、现有 Linux 测试/安装校验驱动、独立真实业务 Playwright 配置及测试目录、WORK-050 文档。生产执行器、Java/Web业务、contracts、原服务器及本机配置禁止修改。

## 阶段与顺序

1. TASK-109：展开用例清单，接入普通Python/语法检查及统一报告格式，冻结现有断言与计量依据。
2. TASK-110：自动准备当前构建和锁定rootfs，接入真实Linux边界、资源、1000次、并发/故障套件及严格回收。
3. TASK-111：独立VM安装当前原生节点，自动完成权限/限额/缺文件/三服务崩溃/卸载恢复对照。
4. TASK-112：新建五服务、数据库、Kafka及Linux节点的业务环境，正常注册、数据部署、校准、发布，再接真实自定义/正式提交/代码回看。
5. TASK-113：必需汇总、防假绿和取消验证，当前基线两次完整通过，独立复核工作流权限与清理，交付WORK-049回归契约。

## 并行与依赖

109先冻结报告和清单；110后111；112复用111的原生启动能力；113等待全部结果。不同CI job可并行，但同VM中固定资源测试串行。文档不授权启动子智能体，独立复核执行方式在实施时落实，仍遵循现有委派规则。

## 迁移与交付

本轮仅文档；人工意图闸通过并明确允许实施后才能写工作流和测试驱动。基线源码4a166d6仅作参考，自动化新增后形成新的完整harness SHA并两次执行；在后续获准提交/推送或手动触发时记录真实运行链接，不伪造CI完成。WORK-049源代码重构开始前先完成本工作基线；重构最终提交用同清单重新验证。

## 风险

新环境还没运行特权链；不能承诺某个runner标签必然通过。旧脚本硬编码、原生安装副作用、业务凭据/端口和测试报告泄漏风险按DESIGN-044治理。精确写路径由TASK约束，需要扩大生产代码范围时另立修复材料。

## 验证

每阶段本地无特权单测和结构校验，再在一次性VM运行对应套件；报告须证明每个必需case执行。113至少做一次可控失败、缺报告、缺能力和取消实验，后续有效基线连续两次通过。重启及其他平台不作为PASS项，阅读验收和独立源码复核不由CI替代。

## 回退

回退本工作新增workflow/job与驱动修改，恢复原六项CI；不撤销WORK-048实现或人工验收。CI退出按所有权清理独占资源，保留脱敏证据；当前服务器、旧ZIP、本机IDEA始终不触碰。若自动化未完整通过，WORK-049不得把旧手工报告当作本次重构验证。

## 变更记录

- 2026-09-10：状态变更：draft → review。原因：已盘点既有验收与CI缺口，补齐分层方案、边界及验收条件供人工审核；尚未实施
- 2026-09-10：结构与内容校验通过，由工具置为 checked。

## 连续请求异常的文档分支（2026-09-10）

第三轮CI仍出现helper在线时连接被重置，已超出测试采样修正。依本计划“真实缺陷另拆边界”的约束，为TASK-110增加仅限development/works/WORK-051的文档读写范围，使用工作项工具维护关联控制元数据；不扩大TASK-110的生产代码权限。WORK-051仅形成复现、修复候选、精确边界和验证计划，保持待审核；必须独立签署意图闸并允许实施后才可修复。当前CI保留失败，TASK-111～113仍等待内核基线成立。

## 语言功能测试期限提案（2026-09-10）

b03e6bd 的 CI 中原 Java 语言功能测试在 5 秒失败，而同轮 Linux 63 项通过。新增待审 TASK-115，精确限定 languages_e2e_test.go 的失败诊断与可信示例编译期限，不修改生产默认值、资源限额或既有功能断言。当前仅文档，需用户明确确认测试专用 30 秒编译期限后才可实施；不能借 TASK-114 或已有推送授权直接修改。

## TASK-115 按用户反馈收窄实施（2026-09-10）

用户要求先确认原因、优化并给出最终结论，再决定是否采用测试专用期限。本轮只实施诊断和有界对照，不采纳 30 秒。先为 TASK-115 增加精确的手动诊断工作流与 Python 驱动读写范围，再编码。保留原测试及生产默认 5 秒，补齐 Usage/Wait 事实；在一次性 Linux VM 编译真实测试二进制，记录首次与后续样本、javac/jar 分段 CPU/墙钟，允许对 JVM 启动参数做显式实验但不修改默认工作流或生产注册命令。原失败保留，重复采样只作测量，不覆盖基线结论。

## TASK-115 追加多轮验证与条件实施（2026-09-10）

用户在看到首轮诊断与测试专用上限建议后，明确要求多次验证；若解释成立，允许仅在测试环境适当放松编译墙钟。这是本轮条件实施授权，不再将30秒视为无条件默认。先追加两轮、每轮3台一次性Linux VM，以交替分配的普通首次/仅预读JDK文件对照检验文件加载等待：预读不执行JVM、不改变命令，不删除系统缓存，所有失败保留。每台继续记录首次和后续样本以及系统PSI，明确系统指标的归属限制。

TASK-115现有写路径不扩大：仅在languages_e2e_test.go加入测试二进制专用编译期限参数（初始默认5秒，运行不变），diagnose_language.py及其自测支持显式样本期限和有界JDK预读，独立workflow支持交替分组。源码、产物及功能断言不变；原5秒负例固定显式执行，不随候选上限变动。若多轮首次/后续和文件预读对照支持加载等待解释，再只将功能测试默认编译上限提高至15秒；用第三轮3台未预读的新VM与正式CI验证，必要时根据实测说明再评估30秒，不无限放宽。若对照不支持解释或仍出现其他失败，保留5秒默认并继续定位，不自动触发条件授权。远端发布沿本批TASK-115已授权的commit/push与一次性CI路径，不连接现有服务器，不改ci.yml、生产或严格资源测试。

对照判定在运行前约定：两轮中，文件预读组的首次Java编译中位墙钟均低于各自未预读组，预读本身有实际读取字节/耗时；原配置后续样本也应重复表现出明显降低。记录所有失败和反例，不把5秒截断值当自然完成时长，不把系统PSI当Java独占统计。这只能支持工具链文件加载对首次延迟的贡献，不宣称找到了所有历史失败的唯一底层原因。

为控制“多等一会儿”本身的混杂，两组均从测试二进制构建结束起等待30秒再开始首次执行；预读组只在这个窗口读取文件，未预读组纯等待。记录实际窗口耗时，预读超出30秒即实验失败；窗口不进入编译计时，也不进入正式CI。

最终一致性检查发现ci.yml的说明仍写“夹具仍使用原5s”，与已批准实施的测试默认15秒不符。先将TASK-115写路径精确扩为该文件的这一处注释维护，禁止改变任何job、执行命令或环境；移除注释内重复期限值，让测试代码继续作为唯一取值来源。此为本轮条件实施的配套修正，不扩大CI执行范围。

## 用户确认实施顺序（2026-09-10）

用户明确选择先完成全部CI，再整体重构，不采用核心基线先行与重构交叉推进的备选。继续原109→110→111→112→113顺序：先完成WORK-051独立复核并交接helper修复；原生部署10项、真实业务15项和必需汇总全部实施，93项回归及原有工程CI在同一基线连续两次完整通过后交付。WORK-049仍保持原TASK-113前置依赖，源码重构不开工；人工意图/验收闸由用户签署。机器重启、其他平台和人工复核继续按原分类，不计为自动化PASS。

## TASK-111权限对照的启动事实修正（2026-09-11）

第三轮部署CI的helper日志证明快速逐项删减触发systemd启动频率限制，而Result保留此前exit-code；原verify-capabilities.py只断言Result不是start-limit-hit，不能证明每轮真的启动过。TASK-111已有该脚本的精确写边界，实施前明确适配：每轮仅对本轮拥有的helper执行reset-failed以隔离主动失败对照的启动计数，不修改unit的频率、资源或capability配置、不重试失败；记录且要求新的InvocationID和主进程启动时间，再判断启动拒绝。对照结束、恢复原文件及摘要后只清除本轮主动失败计数再正常启动；所有原权限、输出、后代和恢复断言保留。该修正只针对测试编排，不能把自动reset带入生产安装器或服务。

## 新内核通信失败的文档分支（2026-09-11）

a0aea38的真实内核边界在event()中返回EINTR；2026-09-11实施核对撤回对Recvmsg的确定归因，该日志同时可能来自测试Poll；这超出TASK-111测试编排边界。按真实缺陷另拆原则，先为TASK-111仅增加development/works/WORK-052文档读写范围，建立独立复现、候选修复、精确生产边界及验证方案，不增加任何Go源码写权限。WORK-052保持待审核、待人工意图闸，当前只能文档和只读归因；先完成已授权原生验证，尚未解决的内核失败继续阻止TASK-112及整体基线成立。不能以另一轮内核通过替代此问题的处理。

## TASK-112验收交回与实施（2026-09-11）

用户已签署WORK-052验收闸，并明确开始真实业务闭环CI。先为TASK-112增加WORK-052文档的读取与工具刷新交回路径，仅用于按已签闸刷新/归档状态，不改其实现或审批。新增根compose.yaml只读用于核对已有依赖镜像与配置，不能执行该用户部署配置。TASK-112仍仅修改CI、独立Playwright测试及工作记录，生产路径禁止不变。

业务栈采用一次性VM内的独占Docker依赖与受限systemd Java服务，复用正常非local配置和本轮新密钥/凭据，所有服务/路径/容器在启动前登记所有权；正常API准备后由单worker、零重试的Playwright串行执行。资源/日志/清理细节先记DESIGN-044，再实施。当前TASK-112实施授权已存在；本批独立复核委派、commit/push及Actions发布不复用TASK-116的单批授权，完成可审阅实现后再请求。

- TASK-112实施细化已写DESIGN-044：追加tsconfig.node.json和eslint.config.js的测试目录索引写边界，使真实页面用例接受现有类型/lint规则；其余前端生产与构建配置边界不变。
