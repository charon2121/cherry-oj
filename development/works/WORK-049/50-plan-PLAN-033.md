---
id: "PLAN-033"
type: "plan"
title: "按命令执行顺序重构 Go 判题引擎源码"
status: "review"
work: "WORK-049"
owners: ["codex/root"]
depends_on: ["CHANGE-013", "DESIGN-043", "DECISION-027"]
related: []
implements: []
verifies: []
tags: []
created_at: "2026-09-10"
updated_at: "2026-09-10"
---

# PLAN-033：重构与阅读验收计划

## 目标

落实 CHANGE-013，以 DESIGN-043 为具体方案，保留可逐项比对的行为与权限基线。
本轮只交付文档；所有 TASK 保持 todo，未发生实施。

2026-09-10 用户新增要求：先把 WORK-048 已通过测试整理成 CI，再据此验证重构。自动化由 WORK-050 承接；TASK-105 开始修改执行链前依赖 TASK-113 的完整基线交付，TASK-107 必须在重构最终提交上重新运行同一清单。CI 不能代替本工作的可读性及独立源码复核。

## 改动区域

Go sandbox 执行链、本机协议、Judge 主线与 node、配置和限额表达、相关包测试与阅读入口。
不改 contracts、Java/Web、部署文件、Go 依赖和全局编码规范。精确边界由 TASK front matter 给出。

## 阶段与顺序

1. TASK-104：在获得后续实施授权后，交接 WORK-048 重叠路径；冻结 HEAD 与未提交 diff/新增文件清单，形成符号导航、资源归属及数字清单。冻结实际文件状态，不能只记 HEAD。
2. TASK-105：沿 /run 重构 sandbox。按协议/数值命名、客户端主线、helper 启动监督回收、init/exec 交接顺序形成独立可编译批次。敏感路径每批核对不变量。
3. TASK-106：整理 Judge 调用执行链的入口、每次判题共享状态、node 旁路，以及配置与限额的阅读性。保持原有验证行为和数值。
4. TASK-107：独立阅读复核与相关回归；逐项记录 AC，列候选规范人工判断项，提供回退范围。

## 并行与依赖

按上述顺序执行；WORK-048 重叠路径先完成交接再改。用户本轮未授权新建子任务会话或运行代理。
独立复核由后续未参与相应实现的审查者完成，实施时落实人员/执行方式，本文不触发任何委派。
全部 TASK 的上游文档需通过意图闸并获得后续明确实施授权，任务才能进入 ready。

## 迁移与交付

交付小批次 diff、更新后的包内阅读入口与实际验证证据，不提交、不推送、不部署，除非用户另外要求。
移动或重命名符号要同步调用点与测试；协议类型提取要比较原序列化结果；不混合修改协议版本、固定数字和位置。
规范候选仅在 DESIGN-043 维护，交付时请人决定哪些值得作为长期规则；迁入 docs/engineering 是后续授权动作。

## 风险

WORK-048 已于 2026-09-10 验收并推送，基线为 4a166d60a163f5d4afee2c7373d73160e11b1e68；TASK-104 实施时仍须重新核对实际工作区及 WORK-050 后续测试变更，不能用“恢复整个目录”回退本工作。
Linux 特权路径不能靠 macOS 的测试通过证明；不可因文档已列测试矩阵就声称测试已运行。
阅读主线必须保留真实并发和清理失败分支，不以减少代码量替代目标。

## 验证

- 每批：gofmt、相关包静态检查和测试；涉及并发必用 race；仅新增能验证协议/生命周期不变量的必要用例。
- 完整 Go 回归：在 apps/judge-engine 执行 gofmt -l .、go vet ./...、go test -race ./...。
- Linux 编译检查：CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go vet ./...。这不是 Linux 实机执行证据。
- Linux 实机：沿 WORK-048 已有夹具验证启动/握手、正常命令、超时/取消、后代回收、产物、FD 和清理失败；明确机器、权限、命令及跳过项。没有可用且已授权环境时记为未执行。
- 完整自动回归：使用 WORK-050 交付的基础、真实 Linux、原生部署和真实业务清单。重构前后对比相同断言及协议/预算/清理不变量，候选的源码SHA与报告SHA必须一致，必需项skip或未执行不能完成本工作；环境指纹变化仍正常重新注册与校准。
- 阅读验收：审查者从 handleRun 出发走到 execve 和最终 HTTP 响应，另走一次判题；回答 AC-001～003；卡住的交接点必须回到源码调整。

## 回退

以 TASK-104 冻结的带未提交内容的基线为起点，仅撤销本工作明确记录的 diff。
不使用 git reset --hard、整个目录 checkout 或删除未跟踪目录恢复基线；不得影响 WORK-048 和用户改动。
发现语义变化或隔离回归立即停止后续批次，保留证据并撤销对应批次；本工作无远端部署回退动作。

## 变更记录

- 2026-09-10：状态变更：draft → review。原因：已完成执行主线、数字语义与等价边界的文档提案，提交人工审核；不授权实施
