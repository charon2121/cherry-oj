---
id: "MEMORY-039"
type: "memory"
title: "修复沙箱启动通信被信号中断时的处理"
status: "checked"
work: "WORK-052"
owners: ["codex/root"]
depends_on: ["VERIFY-053"]
related: []
implements: []
verifies: []
tags: []
created_at: "2026-09-11"
updated_at: "2026-09-11"
---

# MEMORY-039：通过记录不能覆盖新观察到的中断

## 背景

WORK-050把手工验证接入CI，已多轮通过的内核事件等待路径随后出现EINTR，原日志不能区分Poll与Recvmsg。

## 决定与原因

用户已批准最小控制消息修复，实施先分阶段复现；不在测试中忽略或重跑整请求。

## 尝试与教训

真实失败与“预期拒绝”必须区分；不能把启动事件尚未收到的系统调用错误当作已完成权限/握手验证。

## 已知问题

控制通道已发布最小修复，确定性收发与关闭/期限检查、构造条件下真实Linux信号恢复通过；最早失败的具体信号和系统调用仍无法追溯。原生安装曾在128MiB驱动上限处OOM，随后新VM通过但持续触顶；仅CI安装驱动256MiB例外由用户确认后，两台同源码新VM完整CI通过，仍有文件页触顶，不能称为已确证最早OOM根因或杜绝未来失败。技术完成，人工验收交回待用户签署。

## 重新考虑条件

最小用例证伪候选、发生FD重复/泄漏或期限延长时回到设计，不扩大循环重试。

## 变更记录

- 2026-09-11：状态变更：draft → review。原因：保留失败与推断边界，尚无实现结论

- 2026-09-11：t.Helper会把多个内部系统调用的错误归到同一调用行；没有分阶段日志时不能认定Recvmsg是故障来源。本轮保留历史失败，增加诊断后再决定修复点。

- 2026-09-11：新诊断初版在Linux因x/sys Ppoll固定传零sigsetsize而EINVAL，交叉构建无法发现该内核ABI行为；修正测试内核掩码长度后真实信号诊断通过。诊断确认等待阶段可先于ReceiveEvent产生EINTR，随后消息/FD完整。此证据不能倒推历史失败必定来自Poll，更不能证明生产Recvmsg已具备恢复能力；下一步必须分别验证，不能以九项CI绿灯自动结案。

- 2026-09-11：b3e5ec0真实Linux验证63内核/52必需Go全部通过，FD引用由RawConn.Control保护，每次中断后重新获取引用；取消仍依赖原shutdown和组清理链，不承诺Close单独唤醒阻塞socket。真实接收信号用例通过测试专用SO_RCVTIMEO构造可中断条件，不推断生产常态频率。
- 2026-09-11：原生安装驱动OOM与Go控制通道不能混为一个结论；安装预算包含其子进程与文件页，先用组外有界观测区分。已通过轮次同样峰值128MiB，不能因下一轮通过而抹去失败；崩溃后的回执也可能截断，诊断应局部容错，继续独立服务取证。

- 2026-09-11：b699604两台新VM现有九job全过，63内核/52必需Go/10原生及全部清理复验通过；安装均峰值256MiB、oom0，以文件页为主要组成。用户仅批准安装驱动例外，其余管理驱动和正式服务限额保持不变；受限峰值不等于自然需求，不因缓存触顶继续加预算。完整93项还缺WORK-050业务与汇总，不能提前开始WORK-049。
- 2026-09-11：结构与内容校验通过，由工具置为 checked。
