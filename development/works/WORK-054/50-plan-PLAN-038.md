---
id: "PLAN-038"
type: "plan"
title: "为CI软件包下载增加有界同源地址回退"
status: "checked"
work: "WORK-054"
owners: ["codex/root"]
depends_on: ["ISSUE-018", "DESIGN-048", "DECISION-032"]
related: []
implements: []
verifies: []
tags: []
created_at: "2026-09-11"
updated_at: "2026-09-11"
---

# PLAN-038：先证明连接回退，再恢复CI

## 目标

解除同源部分连接失败造成的准备阻断，保留完整校验和原业务失败。

## 阶段与顺序

1. 人工审核材料并签署意图闸、允许实施。
2. 在现有下载单测及新的transport_test.py中证明首TLS网络失败、后续地址正常的旧红。
3. 实施显式参数和标准库连接适配，完成错误/期限/证书/资源负例；默认调用保持。
4. 独立复核后发布本批修复，在一次性GitHub VM验证全部锁定包与原有Linux套件；恢复后推进已有TASK-112浏览器诊断。
5. 记录源码、peer、哈希和实际结果，人工验收后交回WORK-050。未完整通过不冻结重构基线。

## 改动区域

TASK-118精确读写路径为准：rootfs下载入口、新连接适配及测试，CI准备和其测试/README，WORK-054及WORK-050记录。包锁、安装器、生产业务与系统配置禁止修改。

## 并行与依赖

实现和网络验证串行；独立复核委派及本修复发布需要用户在本工作授权，不继承TASK-112测试修正批次的生产发布许可。

## 迁移与交付

无业务或数据迁移；本地单测/构建，远端只使用一次性GitHub VM。不安装个人配置或操作原服务器。

## 验证

控制DNS/socket/时钟复现和真实受控TLS拒绝验证，再运行现有基础测试。保持锁摘要、源域名和下载期限；真实CI成功才认定恢复。所有attempt失败保留。

## 回退

取消CI新增参数即恢复原连接行为；其余实现可独立回退，无数据迁移或用户服务重启。

## 风险

当前只有本地端点对照，未取得GitHub失败peer，须记录归因限度。若这项策略不能解除阻断，保留失败并停止扩大范围。

## 变更记录

- 2026-09-11：状态变更：draft → review。原因：实施顺序、读写路径及验证交回完整，当前只文档
- 2026-09-11：结构与内容校验通过，由工具置为 checked。

## 已批准的诊断阶段（2026-09-11）

用户在阅读VERIFY-055及明确确认请求后批准有界逐包诊断，满足本计划“失败后重审”的要求。按DESIGN-048补充落实diagnostics.py及测试，TASK-118增加这两个精确写路径，其他禁止边界保持。恢复任务后先实现和本地正反例，再沿用本工作已获得的独立复核、修正后提交推送及处理Linux CI授权进行一次诊断实跑；不以重跑刷绿。日志事实若仍不足则如实记录，不顺手修改下载策略。

## 用户批准的方案1预算调整（2026-09-11）

用户阅读响应慢方案比较后明确要求“先按照方案1进行调整，再尝试”。本次仅将ci/prepare.py的下载命令总期限240秒调整为600秒，作为CI冷下载试验；这是对上文“保持240秒/不增加总预算”的明确、有限例外。连接/TLS共享30秒、每地址5秒、读取30秒、4并发、包大小/SHA256、诊断上限和HTTP不重放保持。未引入缓存、共享包、镜像源或慢响应重试，也不修改正式沙箱及编译运行限额。回退为仅恢复该命令240秒及对应测试期望。

验证按先本地现有预算接线测试及基础回归、独立复核、发布、两轮独立GitHub冷下载执行。两轮均保留全部成败，不能只挑成功重跑；每轮kernel/native/business各从空包目录独立下载，最多6个VM样本。若第一轮出现新实现错误先处理或如实中止，不能靠下一轮掩盖。沿用本WORK已获得的复核、提交推送及处理CI授权。

外层job仍为kernel15分钟/native20分钟/business40分钟，本次不修改workflow边界。600秒下载将kernel最紧的总预算保留300秒给构建、测试、清理与上传；依据前轮完成事实可试验，但各子命令最大值之和超过job上限，不能保证所有步骤同时接近各自上限时完成。外层触限必须计失败并停止扩预算，不能把缺失清理报告当成功。
