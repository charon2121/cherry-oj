---
id: "DECISION-032"
type: "decision"
title: "为CI软件包下载增加有界同源地址回退"
status: "approved"
work: "WORK-054"
owners: ["codex/root"]
depends_on: ["DESIGN-048"]
related: []
implements: []
verifies: []
tags: []
created_at: "2026-09-11"
updated_at: "2026-09-11"
---

# DECISION-032：下载故障处理边界

## 要决定什么

是否允许仅CI显式启用有限同域名地址回退，以处理单个连接端点失败；保持下载来源、签名校验和总期限。

## 候选方案

1. 继续完整job重跑：已三轮失败，无法定位也浪费构建资源。
2. 固定当前能用的IP或改镜像源：依赖短期探测，可能绕过既有来源决策，不采用。
3. 使用原域名DNS地址，在共享期限内尝试不同地址；默认部署行为不变。提议采用。

## 决定

方案3为待审核提议。未获得本工作意图闸与实施许可前，不修改下载器或CI执行入口。

## 理由

失败发生在HTTP之前，可以不重放请求、不更换内容而尝试同域名的其他连接。证书失败不可按网络抖动处理。

## 影响与风险

会新增小型标准库HTTPS连接适配，需验证SSL主机名、错误分类、失败socket关闭和期限；不承诺解决所有GitHub网络失败。只支持显式直接HTTPS路径，不能悄悄处理代理或绕开CA。

## 重新考虑条件

实测仍失败、标准库适配需要超出边界的兼容代码、来源包已失效或必须改预算时，停止扩张并另行审核。

## 背景

WORK-050在部署下载器的TLS连接失败上受阻，当前TASK-112无修改该文件权限。只读观察不足以授权修改默认部署行为，因此建立独立、小范围且显式选择的提案。

## 变更记录

- 2026-09-11：状态变更：draft → review。原因：候选与风险完整，等待用户决定，不代签
- 2026-09-11：意图闸通过：review → approved。原因：确认CI同源连接回退方案与边界，允许实施

## 用户批准的方案1预算调整（2026-09-11）

用户阅读响应慢方案比较后明确要求“先按照方案1进行调整，再尝试”。本次仅将ci/prepare.py的下载命令总期限240秒调整为600秒，作为CI冷下载试验；这是对上文“保持240秒/不增加总预算”的明确、有限例外。连接/TLS共享30秒、每地址5秒、读取30秒、4并发、包大小/SHA256、诊断上限和HTTP不重放保持。未引入缓存、共享包、镜像源或慢响应重试，也不修改正式沙箱及编译运行限额。回退为仅恢复该命令240秒及对应测试期望。
