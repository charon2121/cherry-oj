# Text
All body copy, metadata and inline code labels.

```jsx
<Text size="lg" tone="muted">One place for problems, contests and submissions.</Text>
<Text size="cap" tone="meta">Updated 4 minutes ago</Text>
<Text size="sm" mono>O(n log n)</Text>
```

Sizes 18 / 16 / 15 / 14 / 13 / 12px. Tones: default `--fg-2`, strong `--fg`, muted `--muted`, meta `--meta`, accent cherry. Weights 300 / 400 / 510 / 590.
