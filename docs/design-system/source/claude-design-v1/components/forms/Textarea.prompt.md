# Textarea
Multi-line input for issue descriptions and comment composers.

```jsx
<Textarea label="Description" rows={5} placeholder="Add more detail…" />
```

Identical surface to `Input` (0.02 white fill, `--border`, 6px radius, 12px 14px). Only vertical resize.
