# SearchInput
Inline search field and command-palette trigger for app chrome.

```jsx
<SearchInput placeholder="Search issues…" shortcut="⌘K" />
```

Transparent input inside a 0.02-white capsule; 14px glyph, mono shortcut chip at 10px. Focus tints the border cherry.
