# Container
The page column. Wrap every marketing section's content in one.

```jsx
<Container as="section" style={{paddingBlock:'var(--section-y-desktop)'}}>…</Container>
```

`narrow` 760px (prose) · `default` 1200px · `wide` 1440px. Gutters come from the `--container-gutter-*` tokens.
