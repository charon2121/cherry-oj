# Badge
Tiny inline label — version tags, status words, counts.

```jsx
<Badge>v2.4.0</Badge>
<Badge tone="accent">New</Badge>
<Badge tone="success">Shipped</Badge>
```

10px / weight 510, 2px radius, 1px 8px padding. `accent` uses the cherry tint; keep it rare.
