# AGENTS.md

## 新功能开发流程

开发新功能、修复问题、重构或进行其他实现改动时，Agent 必须先执行以下流程：

1. 阅读 `development/WORKS.md`，确认是否已有对应 WORK。
2. 如果没有对应 WORK，先创建 WORK 和本次流程需要的文档。这一轮只编写文档，不修改实现。
3. 文档写完后停止，交给用户审核。用户最初提出开发意图，不等于文档已经通过，也不等于允许实施。
4. 只有用户在后续消息中明确确认文档并允许执行，才能开始编码或执行其他实施任务；实施范围以已确认
   的 TASK 为准。

不得因为用户说“完成这个功能”就省略 WORK、文档审核或 TASK。仅修改 development 文档体系自身时，
可以按照用户的明确要求不新建 WORK/TASK。

本仓库的项目说明与编码规范统一维护在 [`CLAUDE.md`](./CLAUDE.md)。

**开始工作前请先完整读取该文件，并遵循其中的全部约定。** 本文件不再重复其内容，
以免两处描述漂移——`CLAUDE.md` 是唯一真源。
